{
    "service": {
        "name": "{{ consul_service_name }}",
        "port": {{ consul_service_port }},
        "checks": [
            {
                "script": "{{ consul_checks_dir }}/check_port.sh 127.0.0.1 {{consul_service_port}}",
                "interval": "10s"
            }
        ]
    }
}
