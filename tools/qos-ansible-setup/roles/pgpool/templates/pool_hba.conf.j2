# TYPE  DATABASE        USER            ADDRESS                 METHOD
local   all         all                               trust
host    all             all          {{ hostvars[inventory_hostname]['ansible_'+interface].ipv4.address }}/24  trust
host    all             all          127.0.0.1/24  trust
